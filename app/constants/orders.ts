import type { IGetOrdersQuery } from '~/types/api/orders';

export const DEFAULT_ORDER_FILTERS = {
  page: 1,
  itemsPerPage: 10,
}

export const DEFAULT_ORDER_QUERY: IGetOrdersQuery = {
  page: 1,
  itemsPerPage: 10,
}

