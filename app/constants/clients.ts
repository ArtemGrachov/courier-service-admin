import type { IGetClientsQuery } from '~/types/api/clients';

export const DEFAULT_CLIENT_FILTERS = {
  page: 1,
  itemsPerPage: 10,
}

export const DEFAULT_CLIENT_QUERY: IGetClientsQuery = {
  page: 1,
  itemsPerPage: 10,
}

